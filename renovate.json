{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["github>Peculiar-Cloud/renovate"],
  "packageRules": [
    {
      "description": "Keep the tldraw SDK packages on one version. They are released in lockstep and mixing versions breaks the sync protocol, so they are split out of the shared minor/patch group and bumped together.",
      "matchPackageNames": ["tldraw", "@tldraw/assets", "@tldraw/sync", "@tldraw/sync-core"],
      "matchUpdateTypes": ["patch", "minor", "major"],
      "groupName": "tldraw sdk",
      "groupSlug": "tldraw-sdk",
      "automerge": true,
      "labels": ["dependencies", "tldraw", "automerge"]
    },
    {
      "description": "Keep TypeScript within the range supported by typescript-eslint.",
      "matchPackageNames": ["typescript"],
      "allowedVersions": "<6.1.0"
    }
  ]
}
